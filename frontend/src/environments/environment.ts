export const environment = {
  production: false,
  // Node/Express + PostgreSQL API. If you test on a real device or emulator
  // (not just the browser), 'localhost' won't resolve to your machine —
  // use your LAN IP instead, e.g. 'http://192.168.1.5:3000/api'.
  apiUrl: 'http://localhost:3000/api',
};
